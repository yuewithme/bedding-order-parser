# Bedding Order Parser 项目规则

## 项目边界

- 项目目录固定为 `D:\AI-Learning\Projects\bedding-order-parser`。
- `D:\AI-Learning\Projects\Day01` 是冻结边界：不得修改、清理、提交或替换其中内容。
- 开始任务先读取本文件、当前任务指定的合同/报告，并检查 `git status --short`。
- 保留用户已有修改；发现与当前任务相关且未说明的修改时停止并报告。

## 固定输出合同

- 最终业务结果固定为以下 20 个字段，名称和顺序不得改变：`客户`、`币种`、`业务员`、`表头备注`、`行号`、`物料编码`、`物料名称`、`规格`、`颜色`、`面料`、`面料-涤棉成分`、`款式`、`加标方式`、`尺寸类型`、`数量`、`行备注`、`计划发货日期`、`包装方式`、`是否绣花`、`相似分数`。
- 前 19 个字段必须是字符串；`相似分数`必须是 JSON number。正式业务 JSON 禁止 `null`、缺字段、额外字段、诊断状态和人工说明。
- 两种解析模式都必须最终生成五类核心 JSON：正式业务、解析诊断、字典验证、物料候选、物料匹配摘要。

## 两种解析模式

- `standard`：Python 确定性解析、诊断、字典验证和物料匹配；默认不调用 LLM。
- 标准模式的 AI 只允许用户在已完成 Job 上明确确认后触发单条记录复核；结果只能保存为只读 Sidecar，不得修改五类核心 JSON、正式结果或 ERP。
- `ai_enhanced`：整单 AI 是业务字段主解析器；必须经过本地结构预处理、证据校验、Python shadow 对照、字典验证和现有物料匹配后，才允许发布五类核心 JSON。Python shadow 只作为独立对照和 AI 缺值时的本地补空来源。
- `parse_mode` 是 Job 创建后不可变的枚举。AI Job 需要使用标准解析时，必须从其可信原始上传创建独立的 `standard` Job；原 AI Job 不得原地改写模式、状态或结果。历史同 Job 回退数据仅作只读兼容，禁止产生新的模式 mutation。
- AI 结构识别采用本地优先：先在本地识别 Sheet、使用区域、合并单元格、表头和订单区块；只有结构有歧义时才调用 AI。

## 字段与AI边界

- AI 整单模型只提取 17 个业务字段：`客户`、`币种`、`业务员`、`表头备注`、`物料名称`、`规格`、`颜色`、`面料`、`面料-涤棉成分`、`款式`、`加标方式`、`尺寸类型`、`数量`、`行备注`、`计划发货日期`、`包装方式`、`是否绣花`。
- `行号`由本地根据已确认的源 Sheet/行坐标确定性生成，不交给模型生成或改写。
- AI 的每个非空字段必须引用同一订单范围内的 Sheet、单元格/范围和原始文本，并通过本地身份、范围、规范化、类型和业务校验；无证据保持空值并标记状态。
- AI 与 Python 的业务值不同必须形成可审计 review；在 `ai_enhanced` 模式中，技术上可信的 AI 值默认作为正式业务值，Python 只用于对照或 AI 缺值时的本地补空。高风险客户、数量、日期等字段只提高 review 醒目程度，不得仅因业务差异阻断 canonical、下游或五类发布。身份、scope、evidence ownership、正式 20 字段和原子发布等技术完整性失败仍必须 hard block。
- `物料编码`和`相似分数`只能由现有物料匹配层生成。输入证据中确实出现的物料编码文本可以被 AI 作为原文证据引用，但 AI 不得生成、确认、写回、覆盖或用于 ERP。
- AI 不得输出私有思维链、无来源字段、完整原始 HTTP 响应或未经校验的候选。

## 数据安全与调用

- 不发送或保存 API Key、Authorization、本机绝对路径、无关联系人、完整 Excel 二进制、完整请求/响应或私有思维链。
- 真实 PI 业务内容只在用户明确授权的真实 API 验收中按最小证据范围发送；未授权不得调用外部 API。
- 不提交真实 Job、真实 Sidecar、原始 Provider 响应、真实 PI、密钥或本地凭证。
- 不调用 BGE-M3、FAISS 或真实 API，除非当前任务明确授权并在报告中记录调用范围；不因查看页面自动触发 AI。

## Git、测试与交付

- 修改前检查 HEAD、分支和工作区；只暂存本轮必要文件，禁止 `git add .`。
- 保留两份既有恢复文档为未跟踪文件，不暂存、不提交、不清理。
- 未经明确授权不得 `reset`、`restore`、`checkout`、`clean`、`push`、`tag` 或 `amend`。
- 测试按风险运行定向测试；完整 pytest、真实 PI、BGE-M3、FAISS 和真实 API 不是默认步骤，未执行必须在报告中写明。
- 每轮 Gate 生成且只生成一份 `docs/reports/` Markdown 报告；报告记录实际 HEAD、范围、验证、未执行事项、提交和工作区。
- 提交前运行 `git diff --check`，审查 staged 文件和敏感信息；提交后重新核验 HEAD、工作区和报告路径。
